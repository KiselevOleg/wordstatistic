spring:
    application:
        name: user
    datasource:
        url: jdbc:postgresql://user-postgres:5432/user
        username: user
        password: password
    jpa:
        show-sql: true
        properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
        generate-ddl: true
    data:
        redis:
            database: 0
            host: user-redis
            port: 6379
        #    username: user
        #    password: password
