spring:
    application:
        name: user
    datasource:
        url: jdbc:postgresql://localstatistic-postgres:5432/user
        username: user
        password: password
    jpa:
        show-sql: true
        properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
        generate-ddl: true
